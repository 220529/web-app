const path = require("path");

const srcPath = path.join(__dirname, "../", "src");
const distPath = path.join(__dirname, "../", "dist");
const tamplatePath = path.join(__dirname, "../", "templates");

module.exports = {
  srcPath,
  distPath,
  tamplatePath,
};
